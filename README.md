# Quick Python Environment Setup

Minimal bootstrap package for the MVP CLI and test harness.

## Development

Run the focused bootstrap tests with:

```bash
pytest tests/test_bootstrap.py -q
```

Try the CLI help with:

```bash
python -m quick_env_setup --help
```

