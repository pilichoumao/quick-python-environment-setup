# Claude Code Prompt Example

```text
Use Quick Python Environment Setup for this repository.
Start with:
python -m quick_env_setup --source <path_or_url> --dry-run

Default to Level 2 behavior and do not run demo code unless I confirm.
```
