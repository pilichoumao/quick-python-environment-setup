# Qoder Prompt Example

```text
Use the repository CLI as the source of truth.
First run:
python -m quick_env_setup --source <path_or_url> --dry-run

Then summarize the plan and wait before executing any project code.
```
